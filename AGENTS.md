# 前端工程与设计约定

## 用户已确定的工作方式

- 在开发分支完成修改，不在 `main` / `master` 上开发。保留工作区中用户的其他修改。
- 每项需求同时考虑 H5、微信小程序、iOS 和 Android。编译通过与真机验证分别报告，不把浏览器模拟当成真机测试。
- 新增及修改的用户文案接入现有语言系统，覆盖简体中文、繁体中文、英语、俄语、日语、韩语；不得展示翻译键名。

## 设计规范

用户指定采用 Emil Kowalski 的 Design Engineering 技能：

- 官方来源：https://emilkowal.ski/skill
- 技能源码：https://github.com/emilkowalski/skill/tree/main/skills/emil-design-eng
- 本机安装：`C:/Users/Administrator/.codex/skills/emil-design-eng/SKILL.md`

涉及前端组件、样式或动效时先阅读该技能。在其他开发环境中，从同一官方来源安装或阅读，保留这些工程约定。

- 遵循现有白色、暖灰、黄色的产品视觉体系，统一字级、间距、圆角、按钮和状态组件。
- 明确区分加载中、正常空数据、请求失败和加载更多失败；保留可恢复操作，并在提交中防止重复触发。
- 高频操作优先即时响应。动效必须有用途，不新增重复换卡入场动画或装饰性等待。
- 点击反馈轻微而迅速，过渡明确指定属性；避免 `transition: all`，尊重减少动态效果偏好。
- 用户要求：后续新增弹层统一从下往上展开、从上往下收起，遮罩淡入淡出；收起动画结束后再卸载，取消、确认、系统返回均遵循此规则。编辑分组和未保存确认等嵌套弹层也需过渡。可复用 `components/common/SlideUpPanel.vue`，H5、微信、App 同步考虑。
- 公共状态组件应有合理默认样式，文本可换行，窄屏与长语言不溢出，按钮具备清晰标签与足够触控面积。
- 采用 uni-app 的跨端组件和已验证的 CSS。技能中的 React、DOM、WAAPI、`@starting-style` 等示例仅代表思路，不能直接引入不支持微信小程序的实现。
- 保留现有业务判断与接口契约，样式修改不扩大到无关模块。

## 验证与环境

- 用户最新要求：默认只完成需求改动，验证由用户负责；不主动运行测试、构建或浏览器/小程序验证，除非用户再次明确要求验证。

- 按改动执行相关行为回归，并构建 H5、微信小程序和 App；视觉调整检查窄屏、长文本与各状态截图。
- 对不可用的真机或环境如实说明，不能宣称未执行的验证已经通过。
- 本地调试接口与上传体验包的公网接口分别核对。保留用户选择的源码接口环境，不在未说明的情况下切换；手机中的 `127.0.0.1` 不指向开发电脑。
- 前端页面出现 500 时先核对后端日志、数据库迁移和实际请求地址，不能把服务错误掩盖成正常空数据。
